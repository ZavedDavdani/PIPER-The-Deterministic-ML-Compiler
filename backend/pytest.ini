[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    ollama: requires a real local Ollama server (gated by PIPER_RUN_OLLAMA_TESTS=1; skipped by default, never required for the normal deterministic suite)
