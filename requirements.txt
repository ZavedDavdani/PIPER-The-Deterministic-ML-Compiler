# Core data science and ML
# Versions pinned to the exact versions verified for PIPER.
pandas==3.0.2
scikit-learn==1.8.0

# Data validation and schemas
pydantic==2.13.4

# Testing
pytest==9.1.1

# Agent orchestration
# Used by PIPER's stateful planning and execution graph.
langgraph==1.2.10

# Cloud LLM Provider (OpenAI SDK)
openai==3.3.1

# Cloud LLM Provider (Google Gemini SDK)
google-genai==2.20.0

# Backend API
# FastAPI exposes the PIPER API and SSE endpoints.
fastapi==0.141.1
uvicorn==0.52.1

# Environment configuration (.env loading at backend startup)
python-dotenv==1.2.3

# Multipart dataset uploads
python-multipart==0.0.32

# HTTP client
# Used by FastAPI's TestClient and HTTP-based integrations.
httpx==0.28.1

# Multi-format dataset ingestion
# openpyxl -> .xlsx
# xlrd     -> legacy .xls
# pyarrow  -> .parquet
#
# CSV, TSV, JSON, and IPYNB are handled by pandas / Python stdlib.
openpyxl==3.1.5
xlrd==2.0.2
pyarrow==25.0.1